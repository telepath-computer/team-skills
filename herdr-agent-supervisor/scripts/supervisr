#!/usr/bin/env python3
"""supervisr — agent supervision on herdr. See ../SKILL.md."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from supervisrlib.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
